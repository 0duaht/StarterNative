import { createAction } from 'redux-actions';
import Types from 'constants/types';

export function setHomeView(view){
  return createAction(Types.SET_HOME_VIEW)(view);
}

export function setHomeBinding(bindingDone){
  return createAction(Types.SET_HOME_BINDING)(bindingDone);
}