import React, { Component } from 'react';
import { ActivityIndicator, Text, View } from 'react-native';
import styles from 'styles';
import ImageBackground from 'components/image_background'

export default class LoadingScreenBlank extends Component {
  render(){
    return (
      <View style={[styles.loadingScreen, { backgroundColor: 'white' }]}>
        <ActivityIndicator
          animating={true}
          color='black'
          size='small'
          style={{margin: 15}} />
        <Text style={{color: '#fff'}}>{this.props.text || "Loading"}</Text>
      </View>
    )
  }
}