import React, { Component } from 'react';
import { View } from 'react-native';
import Styles from 'styles';

export default class FullContainer extends Component {
  render(){
    return (
      <View style={Styles.full}>
        {this.props.children}
      </View>
    )
  }
}